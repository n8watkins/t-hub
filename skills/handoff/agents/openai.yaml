interface:
  display_name: "Handoff"
  short_description: "Preserve verified state across agent sessions"
  default_prompt: "Use $handoff to prepare a verified zero-context continuation for the next agent."
