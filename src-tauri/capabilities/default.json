{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core capability for the main TermHub window (events + core APIs).",
  "windows": ["main"],
  "permissions": ["core:default"]
}
