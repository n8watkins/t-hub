// STUB — implemented by the Canvas subagent (task #11).
//
// Responsibilities (PRD §5.3, FR-001/FR-002):
//   - Responsive auto-grid of terminal tiles, arbitrary count, no hard cap.
//   - Deterministic insertion after the focused tile, then rebalance.
//   - Add / remove / focus; global "+" to spawn; close tile (detach).
//   - Drive the workspace store; mount <TerminalView visible> only for shown tiles.
export function Canvas() {
  return (
    <div className="flex h-full w-full items-center justify-center text-neutral-500">
      Canvas — not yet implemented (0.1 nucleus scaffold)
    </div>
  );
}
